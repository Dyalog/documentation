# Environment Variables

A configuration parameter can be set as an environment variable. This mechanism, the order of precedence, and the full list of parameters (including those specific to Unix) are documented in the [Dyalog Configuration Guide](../../dyalog-configuration-guide/how-to-set/environment-variables.md).

On Unix, environment variable names are given in upper case, for example `MAXWS`.
