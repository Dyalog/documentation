# Environment Variables

A configuration parameter can be set as an environment variable. This mechanism, the order of precedence, and the full list of parameters (including those specific to UNIX and macOS) are documented in the [Dyalog Configuration Guide](../../../dyalog-configuration-guide/how-to-set/environment-variables).

On UNIX and macOS, environment variable names are given in upper case, for example `MAXWS`.
