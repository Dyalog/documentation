# Object Reference audit

Generated by `tools/buildgui/objref_audit.py` from `objectmodel.json`: 76 objects, 342 properties, 101 methods, 147 events, after the exclusions below.

Findings: 0

No findings.

## Excluded by design

Not defects. Deliberate decisions, encoded in the script, listed here so a clean run means clean.

Members excluded from the guide; present in the interpreter and in objectmodel.json, undocumented on purpose (Windows CE / PocketAPL-era vestiges):

- `EvaluationDays`
- `OKButton`
- `SIPMode`
- `SIPResize`
- `ShowSIP`

Members whose page is not named after them:

- `Index -> index-property.md`

Pages under methodorevents/ documenting Session (⎕SE) members, which fall outside the model because Session is excluded from Objects:

- `afterfix`
- `fix`
- `format`
- `sessionprint`
- `sessiontrace`
- `workspaceloaded`

Object pages outside the model; documented and present in #.ObjectMembers but missing from Objects, so they get no cross-reference tables:

- `netcontrol`

Site directories populated at deploy time and absent from the repository; links into them are not checked:

- `files`

