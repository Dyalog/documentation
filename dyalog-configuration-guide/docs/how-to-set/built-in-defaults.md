# Built-in Defaults

Every configuration parameter has a built-in default that the interpreter uses when the parameter is not set by any other source. The built-in default therefore has the lowest precedence (see [Order of Precedence](order-of-precedence.md)).

A parameter's default can differ between operating systems. Each parameter's own page states its default, and notes any per-platform differences.
