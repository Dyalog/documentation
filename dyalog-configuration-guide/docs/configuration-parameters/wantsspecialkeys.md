# WantsSpecialKeys

!!! Info "Information"
    This configuration parameter is only relevant when using the Unicode edition of Dyalog.

A list of applications (for example, `putty.exe`) that use the command strings in the Input Translate Tables.

Valid values are a list of application names.

<!-- REVIEW(default): default value not present in the migrated source; confirm. -->
