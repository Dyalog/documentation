# SaveLogOnExit

Whether the Session log is saved before Dyalog terminates.

Valid values are:

- `0` : the Session log is not saved on exit
- `1` : the Session log is saved on exit

<!-- REVIEW(default): default value not present in the migrated source; confirm. -->

Related parameters: [SaveSessionOnExit](savesessiononexit.md), [SaveContinueOnExit](savecontinueonexit.md).
