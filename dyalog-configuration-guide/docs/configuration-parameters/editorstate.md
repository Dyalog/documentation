# EditorState

An internal parameter that records the state (normal or maximised) of the last edit window, so that the next edit window is created in the same state. It is not intended to be set by the user.

<!-- REVIEW(default): default value not present in the migrated source; confirm. -->
