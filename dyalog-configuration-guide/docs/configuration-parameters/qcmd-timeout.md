# qcmd_timeout

!!! Info "Information"
    This configuration parameter is only relevant on the Microsoft Windows operating system.

The length of time, in milliseconds, that Dyalog waits for the execution of a Windows command to start.

Valid values are a number of milliseconds.

Default is `5000`.
