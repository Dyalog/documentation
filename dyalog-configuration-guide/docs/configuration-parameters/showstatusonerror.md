# ShowStatusOnError

Whether the Status window is displayed automatically, when required, when Dyalog attempts to write output to it.

Valid values are:

- `0` : the Status window is not displayed automatically
- `1` : the Status window is displayed automatically when output is written to it

<!-- REVIEW(default): default value not present in the migrated source; confirm. -->
