# localdyalogdir

The directory in which Dyalog is installed on the client, in a client/server installation.

Valid values are a directory path.

<!-- REVIEW(default): default value not present in the migrated source; confirm. -->

Related parameters: [Dyalog](dyalog.md), [DyalogInstallDir](dyaloginstalldir.md).
