# SaveSessionOnExit

Whether the current Session is saved in the Session file before Dyalog terminates.

Valid values are:

- `0` : the Session is not saved on exit
- `1` : the Session is saved on exit

<!-- REVIEW(default): default value not present in the migrated source; confirm. -->

Related parameters: [SaveContinueOnExit](savecontinueonexit.md), [SaveLogOnExit](savelogonexit.md).
